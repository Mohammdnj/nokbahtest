{
  "name": "nokba",
  "version": "1.0.0",
  "description": "Nokba rental contract platform — pre-built static export in deploy/",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "build": "echo Pre-built && exit 0",
    "start": "echo Static site && exit 0"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18"
  }
}
